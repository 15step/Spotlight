import * as React from 'react';

const About = () => {
    return(
        <div>
            Spotlight is a service that allows you to keep track of an indvidual
            or organizations' campaign finance contributions
        </div>
    );
};

export default About;